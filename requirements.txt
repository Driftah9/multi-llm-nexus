# Core (always required)
pyyaml>=6.0

# Mattermost adapter
aiohttp>=3.9

# ── Provider SDKs (install only what you need) ────────────────────────────────

# Anthropic (Claude API)
anthropic>=0.30

# OpenAI-compatible (OpenAI, Groq, Mistral, DeepSeek, xAI, Together,
#   Fireworks, Perplexity, HuggingFace, Cerebras, Azure, LM Studio, vLLM)
openai>=1.30

# Google (Gemini AI Studio + Vertex AI)
# NOTE: the new unified SDK is `google-genai` (imported as `from google import genai`).
# The older `google-generativeai` package is deprecated and does NOT provide
# `google.genai`, so src/providers/gemini.py fails to import against it.
google-genai>=0.3

# Google (Vertex AI) — in addition to google-genai
# google-cloud-aiplatform>=1.50

# Cohere
cohere>=5.0

# AWS Bedrock
boto3>=1.34

# Ollama (local, no API key) — uses httpx for HTTP
httpx>=0.27

# ── Platform adapters (install only what you use) ─────────────────────────────

# Telegram
python-telegram-bot>=21.0

# OpenAI-compatible HTTP API adapter (VS Code, Cursor, Continue.dev, Open WebUI)
fastapi>=0.110
uvicorn>=0.29

# ── Optional dev/test dependencies ───────────────────────────────────────────
# pytest>=8.0
# pytest-asyncio>=0.23
trafilatura>=1.6  # on-box web content extraction (replaces remote Jina Reader)

# Optional — only for council failover (multi-orchestrator lease/checkpoint). A floor
# deployment without it simply runs council failover dark (capability-gated).
# redis>=5.0
